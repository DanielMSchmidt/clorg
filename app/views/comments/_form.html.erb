<%= form_for([@message, @comment]) do |f| %>
  <%= render 'shared/error_messages' %>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
